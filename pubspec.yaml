name: flutter_mobx_mvvm_task_manager
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: ^2.3.0
  hive: ^2.2.3
  json_annotation: ^4.9.0
  mobx: ^2.5.0
  path_provider: ^2.1.3
  provider: ^6.1.5+1

dev_dependencies:
  build_runner: ^2.4.0
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  json_serializable: ^6.8.0
  mobx_codegen: ^2.6.0
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
